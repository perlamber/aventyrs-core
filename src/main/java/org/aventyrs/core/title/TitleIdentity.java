package org.aventyrs.core.title;

/**
 * A named Título identity when a rule distinguishes it from its {@link TitleArchetype}. Unlike an
 * archetype, this is deliberately sparse: an identity is added only when a concrete rule needs it.
 */
public enum TitleIdentity {
    BRUXO
}
